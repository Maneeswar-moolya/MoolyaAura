# Generated by ai/knowledge/index.ts - do not edit by hand.
# Metadata only. Read the named file when you need an implementation detail.

pages:
  Base:
    file: tests-e2e/pages/base.page.ts
    purpose: Shared behaviour for every page object.
    methods:
      - goto()
      - url() -> string
  LoginPage:
    file: tests-e2e/pages/login.page.ts
    purpose: "Bugasura sign-in page - https://my.bugasura.io/"
    methods:
      - open()
      - emailField() -> Locator
      - passwordField() -> Locator
      - signInButton() -> Locator
      - forgotPasswordLink() -> Locator
      - termsLink() -> Locator
      - openTerms() -> Page
      - localisedSignInButton() -> Locator
      - languageToggle() -> Locator
      - selectedLanguageLabel() -> Locator
      - languageMenuHeading() -> Locator
      - languageOption() -> Locator
      - languageOptions() -> Locator
      - activeLanguageOption() -> Locator
      - errorMessage() -> Locator
      - currentErrorText() -> string
      - errorText() -> string
      - hasValidationSignal() -> boolean
      - signIn()
      - isOnLoginPage() -> boolean
  ProjectsPage:
    file: tests-e2e/pages/projects.page.ts
    purpose: "Bugasura Projects area - https://my.bugasura.io/apps"
    methods:
      - open()
      - dismissOnboardingModal()
      - createProjectButton() -> Locator
      - projectNameField() -> Locator
      - teamDropdownButton() -> Locator
      - submitButton() -> Locator
      - cancelButton() -> Locator
      - validationErrors() -> Locator
      - validationMessages() -> string[]
      - isFormOpen() -> boolean
      - availableTeams() -> string[]
      - selectTeam()
      - openCreateForm()
      - selectFirstTeam() -> string
      - submit()
      - fillNewProject()
      - isProjectListed() -> boolean
      - createProjectDialogHeading() -> Locator
      - createNewTeamOption() -> Locator
      - openCreateNewTeamForm()
      - createNewTeamHeading() -> Locator
      - teamNameField() -> Locator
      - teamEmailInputArea() -> Locator
      - addTeamMemberButton() -> Locator
      - teamEmailError() -> Locator
      - createTeamCancelButton() -> Locator
      - enterTeamEmail()
      - submitTeamEmail()
      - isCreateTeamFormOpen() -> boolean
  TermsPage:
    file: tests-e2e/pages/terms.page.ts
    purpose: "Bugasura terms of service - https://bugasura.io/terms-of-service"
    methods:
      - heading() -> Locator
  WorkspacePage:
    file: tests-e2e/pages/workspace.page.ts
    purpose: The signed-in Bugasura workspace.
    methods:
      - signedInMarker() -> Locator
      - projectsLink() -> Locator
      - dashboardTab() -> Locator
      - sectionHeader() -> Locator
      - isTabActive() -> boolean
      - isSignedIn() -> boolean

fixtures:
  importFrom: tests-e2e/fixtures.ts
  available: [healing, step, loginPage, workspacePage, projectsPage, bugasuraCredentials, bugasuraEmail, test, trace, requireCredentials, requireEmail, requireDataMutationOptIn, expect]

support:
  data-driven:
    file: tests-e2e/support/data-driven.ts
    purpose: Test-side half of the data-driven contract.
  env:
    file: tests-e2e/support/env.ts
    purpose: Environment configuration for the Bugasura suite.
  generic-form:
    file: tests-e2e/support/generic-form.ts
    purpose: Driving a form from nothing but the workbook row.
  global-setup:
    file: tests-e2e/support/global-setup.ts
    purpose: Clear per-run evidence, then refresh the data-driven cache.
  load-env:
    file: tests-e2e/support/load-env.ts
    purpose: Loads .env, and must be imported before anything reads process.env.
  resilient-locator:
    file: tests-e2e/support/resilient-locator.ts
    purpose: "Resilient locators, and what \"safe healing\" is allowed to mean."
  steps:
    file: tests-e2e/support/steps.ts
    purpose: "Per-step evidence: which step failed, and what the screen looked like."
